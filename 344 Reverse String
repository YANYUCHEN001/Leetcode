class Solution(object):         
    def reverseString(self, s):
        """
        :type s: str
        :rtype: str
        """
        return s[::-1]
        
"""
1.最初我的想法是将字符串转变成列表，然后通过倒叙读取的方法，将字符串打印出来。但是这太复杂了。
2.对字符串的用法有一点忘记了，字符串可以表达成str[]形式去读取。如果记住这一点，那么久容易的多。
所以说记住字符串列表等用法非常重要。
3.字符串可以通过切片来读取。
4.不懂的就要及时弄懂，常读常新
"""
